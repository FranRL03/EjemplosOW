server:
  #  este puerto se utiliza para la configurac�n de la aplicacion
  port: 8888

#  inserta un nombre a una de las aplicaciones que vamos creando
  spring:
    application:
      name: configserver

cloud:
  config:
    server:
      git:
        uri: https://github.com/franrl03/spring-cloud-config
        username: franrl
        password:


